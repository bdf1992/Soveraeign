name: gap 195 operation attribution

on:
  push:
    branches:
      - gap/195-participant-isolation

permissions:
  contents: write

jobs:
  wire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Wire session attribution into the Gateway crossing
        run: |
          python - <<'PY'
          from pathlib import Path
          import json

          def replace(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              if old not in text:
                  raise SystemExit(f'expected block missing in {path}: {old[:80]!r}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8', newline='\n')

          # Gateway contract: session attribution and interface provenance are top-level
          # envelope facts. A domain operation may not smuggle replacements in arguments.
          replace(
              'services/gateway/src/soveraeign_gateway_service/contract.py',
              'AuthorityCheck = Callable[[str, str, str], str]\nServiceRoute = Callable[[str, dict[str, Any], str], dict[str, Any]]\n',
              'AuthorityCheck = Callable[[str, str, str], str]\nAttributionCheck = Callable[[str, str, str, str, str | None], None]\nServiceRoute = Callable[[str, dict[str, Any], str], dict[str, Any]]\n')
          replace(
              'services/gateway/src/soveraeign_gateway_service/contract.py',
              'ATTRIBUTION_ARGUMENTS = frozenset({"actor", "actor_id", "actor_kind"})\n',
              'ATTRIBUTION_ARGUMENTS = frozenset({\n    "actor", "actor_id", "actor_kind", "principal_id", "session_id",\n    "session_binding_id", "interface_binding_id", "interface_operation_digest",\n})\n')

          # Gateway evidence: preserve the caller claim before checking it, then record the
          # attribution decision as its own step. Resolution links to that decision.
          replace(
              'services/gateway/src/soveraeign_gateway_service/evidence.py',
              '''         "actor_kind": mapping.get("actor_kind")
         if isinstance(mapping.get("actor_kind"), str) else None,
         "transport": mapping.get("transport")
''',
              '''         "actor_kind": mapping.get("actor_kind")
         if isinstance(mapping.get("actor_kind"), str) else None,
         "session_id": mapping.get("session_id")
         if isinstance(mapping.get("session_id"), str) else None,
         "session_binding_id": mapping.get("session_binding_id")
         if isinstance(mapping.get("session_binding_id"), str) else None,
         "principal_id": mapping.get("principal_id")
         if mapping.get("principal_id") is None or isinstance(mapping.get("principal_id"), str)
         else None,
         "interface_binding_id": mapping.get("interface_binding_id")
         if isinstance(mapping.get("interface_binding_id"), str) else None,
         "interface_operation_digest": mapping.get("interface_operation_digest")
         if isinstance(mapping.get("interface_operation_digest"), str) else None,
         "transport": mapping.get("transport")
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/evidence.py',
              '''
def record_resolution(record: RecordService, request: dict[str, Any], request_id: str,
                      capability: dict[str, Any], endpoint: dict[str, Any],
                      map_digest: str) -> dict[str, Any]:
''',
              '''
def record_attribution(record: RecordService, request: dict[str, Any], request_id: str,
                       request_entry_id: str, *, decision: str,
                       diagnostic_code: str | None = None) -> dict[str, Any]:
    """Record the session/identity decision separately from authority.

    The interface binding describes how this operation was rendered. The session
    binding describes the host continuity boundary. They are deliberately different
    fields and neither one grants authority.
    """
    payload: dict[str, Any] = {
        "record_kind": "gateway-session-attribution", "request_id": request_id,
        "request_entry_id": request_entry_id, "actor_kind": request["actor_kind"],
        "session_id": request["session_id"],
        "session_binding_id": request["session_binding_id"],
        "principal_id": request["principal_id"],
        "interface_binding_id": request["interface_binding_id"],
        "interface_operation_digest": request["interface_operation_digest"],
        "decision": decision,
    }
    if diagnostic_code is not None:
        payload["diagnostic_code"] = diagnostic_code
    return record.append("EVENT", request["logical_endpoint"], request["actor"], payload)


def record_resolution(record: RecordService, request: dict[str, Any], request_id: str,
                      attribution_entry_id: str, capability: dict[str, Any],
                      endpoint: dict[str, Any], map_digest: str) -> dict[str, Any]:
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/evidence.py',
              '''        {"record_kind": "gateway-capability-resolution",
         "request_id": request_id, "capability_id": capability["capability_id"],
''',
              '''        {"record_kind": "gateway-capability-resolution",
         "request_id": request_id, "attribution_entry_id": attribution_entry_id,
         "capability_id": capability["capability_id"],
''')

          # Gateway core: structural acceptance -> session attribution -> resolution ->
          # authority. Known session refusals remain governed refusals; reader failures
          # remain operational failures.
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''    AuthorityCheck,
    GatewayFault,
''',
              '''    AttributionCheck,
    AuthorityCheck,
    GatewayFault,
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''    record_authority,
    record_request,
''',
              '''    record_attribution,
    record_authority,
    record_request,
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''                 authority: AuthorityCheck, routes: dict[str, ServiceRoute], *,
                 authority_denials: tuple[type[BaseException], ...] = ()) -> None:
''',
              '''                 authority: AuthorityCheck, routes: dict[str, ServiceRoute], *,
                 attribution: AttributionCheck,
                 authority_denials: tuple[type[BaseException], ...] = (),
                 attribution_denials: tuple[type[BaseException], ...] = ()) -> None:
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''        self.authority = authority
        self.authority_denials = authority_denials
''',
              '''        self.authority = authority
        self.attribution = attribution
        self.authority_denials = authority_denials
        self.attribution_denials = attribution_denials
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''            accepted = self._accept(request)
            capability, endpoint = self._resolve(accepted)
            resolution = record_resolution(
                self.record, accepted, request_id, capability, endpoint,
                self.capability_map["input_state_digest"])
''',
              '''            accepted = self._accept(request)
            attribution = self._check_attribution(
                accepted, request_id, request_entry["entry_id"])
            capability, endpoint = self._resolve(accepted)
            resolution = record_resolution(
                self.record, accepted, request_id, attribution["entry_id"], capability,
                endpoint, self.capability_map["input_state_digest"])
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''        required = ("actor", "actor_kind", "logical_endpoint", "transport", "scope", "arguments")
''',
              '''        required = (
            "actor", "actor_kind", "session_id", "session_binding_id", "principal_id",
            "interface_binding_id", "interface_operation_digest", "logical_endpoint",
            "transport", "scope", "arguments")
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''                or not isinstance(request["logical_endpoint"], str)
                or not request["logical_endpoint"].startswith("sov://")
''',
              '''                or not isinstance(request["session_id"], str) or not request["session_id"]
                or not isinstance(request["session_binding_id"], str)
                or not request["session_binding_id"]
                or (request["principal_id"] is not None
                    and (not isinstance(request["principal_id"], str)
                         or not request["principal_id"]))
                or not isinstance(request["interface_binding_id"], str)
                or not request["interface_binding_id"]
                or not isinstance(request["interface_operation_digest"], str)
                or not request["interface_operation_digest"]
                or not isinstance(request["logical_endpoint"], str)
                or not request["logical_endpoint"].startswith("sov://")
''')
          replace(
              'services/gateway/src/soveraeign_gateway_service/core.py',
              '''        return dict(request)

    def _resolve(self, request: dict[str, Any]) -> tuple[dict[str, Any], dict[str, Any]]:
''',
              '''        return dict(request)

    def _check_attribution(self, request: dict[str, Any], request_id: str,
                           request_entry_id: str) -> dict[str, Any]:
        """Prove the session claim before capability resolution or authority.

        Session identity and grant authority are separate questions. A valid session
        never supplies a grant; a valid grant never proves which session made the call.
        """
        try:
            self.attribution(
                request["actor"], request["actor_kind"], request["session_id"],
                request["session_binding_id"], request["principal_id"])
        except Exception as error:
            diagnostic = getattr(error, "reason_code", type(error).__name__)
            if self.attribution_denials and isinstance(error, self.attribution_denials):
                record_attribution(
                    self.record, request, request_id, request_entry_id,
                    decision="REFUSED", diagnostic_code=diagnostic)
                raise GatewayRefusal(
                    diagnostic, str(error), stage="check-attribution",
                    diagnostic_code=diagnostic) from error
            record_attribution(
                self.record, request, request_id, request_entry_id,
                decision="FAILED", diagnostic_code=type(error).__name__)
            raise GatewayFault(
                "ATTRIBUTION_CHECK_FAILED", str(error), event="gateway.check-attribution",
                stage="check-attribution", error_type=type(error).__name__) from error
        return record_attribution(
            self.record, request, request_id, request_entry_id, decision="ALLOWED")

    def _resolve(self, request: dict[str, Any]) -> tuple[dict[str, Any], dict[str, Any]]:
''')

          # The service declaration adds no new public operation. It only makes the
          # attribution precondition and owned evidence explicit on accept-request.
          p = Path('services/gateway/contracts/service.json')
          manifest = json.loads(p.read_text(encoding='utf-8'))
          if 'session-attribution' not in manifest['owns']:
              manifest['owns'].insert(2, 'session-attribution')
          accept = next(op for op in manifest['operations'] if op['operation'] == 'accept-request')
          for precondition in (
              'declared_session_identity', 'declared_interface_binding',
              'declared_interface_operation_digest', 'session_attribution_matches', 'session_live'):
              if precondition not in accept['preconditions']:
                  accept['preconditions'].append(precondition)
          for refusal in ('ACTOR_ATTRIBUTION_MISMATCH', 'SESSION_CLOSED'):
              if refusal not in accept['refusals']:
                  accept['refusals'].append(refusal)
          manifest['local_refusals']['ACTOR_ATTRIBUTION_MISMATCH'] = 'AUTHORITY_REFUSED'
          manifest['local_refusals']['SESSION_CLOSED'] = 'AUTHORITY_REFUSED'
          if 'console:operator-session' not in manifest['depends_on']:
              manifest['depends_on'].append('console:operator-session')
          p.write_text(json.dumps(manifest, indent=2) + '\n', encoding='utf-8', newline='\n')

          # Local composition supplies the concrete Console-backed attribution reader.
          replace(
              'scripts/sovnode/composition.py',
              'from soveraeign_console_service import authority as console_authority  # noqa: E402\nfrom soveraeign_console_service.refusals import AuthorityRefused  # noqa: E402\n',
              'from soveraeign_console_service import authority as console_authority  # noqa: E402\nfrom soveraeign_console_service import reads as console_reads  # noqa: E402\nfrom soveraeign_console_service.refusals import (  # noqa: E402\n    ActorAttributionMismatch, AuthorityRefused, SessionClosed, UnknownRecord,\n)\n')
          replace(
              'scripts/sovnode/composition.py',
              '''        def authority(actor: str, capability: str, scope: str) -> str:
            return console_authority.check(
                self.record.reconstruct(), self.node_id, actor, capability, scope)

        self.gateway = Gateway(
''',
              '''        def authority(actor: str, capability: str, scope: str) -> str:
            return console_authority.check(
                self.record.reconstruct(), self.node_id, actor, capability, scope)

        def attribution(actor: str, actor_kind: str, session_id: str,
                        session_binding_id: str, principal_id: str | None) -> None:
            entries = self.record.reconstruct()
            try:
                session = console_reads.session(entries, session_id)
            except UnknownRecord:
                raise ActorAttributionMismatch(
                    "gateway session attribution does not match a local session") from None
            if (console_reads.foreign(session, self.node_id) is not None
                    or session.get("operator_id") != actor
                    or session.get("actor_kind") != actor_kind
                    or session.get("binding_id") != session_binding_id
                    or session.get("principal_id") != principal_id):
                raise ActorAttributionMismatch(
                    "gateway session attribution does not match the recorded session")
            if session.get("lifecycle") != "OPEN":
                raise SessionClosed(f"session {session_id} is CLOSED")

        self.gateway = Gateway(
''')
          replace(
              'scripts/sovnode/composition.py',
              '''            {"asset:in-process": asset_routes.call,
             "registry:in-process": registry_routes.call,
             "console:in-process": console_routes.call,
             "host:in-process": host_routes.call},
            authority_denials=(AuthorityRefused,),
''',
              '''            {"asset:in-process": asset_routes.call,
             "registry:in-process": registry_routes.call,
             "console:in-process": console_routes.call,
             "host:in-process": host_routes.call},
            attribution=attribution,
            authority_denials=(AuthorityRefused,),
            attribution_denials=(ActorAttributionMismatch, SessionClosed),
''')

          # Node Interface builder: distinguish the interface binding from the host
          # binding recorded on the session, and require explicit session provenance.
          replace(
              'scripts/sovnode/bindings.py',
              '''def invocation_request(document: dict[str, Any], operation_id: str, binding_kind: str,
                       actor: str, scope: str, arguments: dict[str, Any]) -> dict[str, Any]:
    """Build one Gateway envelope; rendering never supplies a route or a grant."""
''',
              '''def invocation_request(document: dict[str, Any], operation_id: str, binding_kind: str,
                       actor: str, scope: str, arguments: dict[str, Any], *,
                       session_id: str, session_binding_id: str,
                       principal_id: str | None) -> dict[str, Any]:
    """Build one Gateway envelope; rendering never supplies a route or a grant.

    ``interface_binding_id`` names this Human/Model rendering. ``session_binding_id``
    names the host continuity binding pinned when the session opened. Keeping both
    prevents two different layers from collapsing into one overloaded binding id.
    """
''')
          replace(
              'scripts/sovnode/bindings.py',
              '''    if record["route_affordance"]["kind"] not in INVOKABLE:
        raise BindingRefusal("OPERATION_NOT_REACHABLE", operation_id)
''',
              '''    if record["route_affordance"]["kind"] not in INVOKABLE:
        raise BindingRefusal("OPERATION_NOT_REACHABLE", operation_id)
    if not session_id or not session_binding_id:
        raise BindingRefusal("SESSION_IDENTITY_REQUIRED", operation_id)
    if principal_id is not None and not principal_id:
        raise BindingRefusal("SESSION_IDENTITY_REQUIRED", operation_id)
''')
          replace(
              'scripts/sovnode/bindings.py',
              '''        "arguments": dict(arguments),
        "binding_id": BINDING_IDS[binding_kind],
        "interface_operation_digest": record["record_digest"],
''',
              '''        "arguments": dict(arguments),
        "session_id": session_id,
        "session_binding_id": session_binding_id,
        "principal_id": principal_id,
        "interface_binding_id": BINDING_IDS[binding_kind],
        "interface_operation_digest": record["record_digest"],
''')

          # Gateway unit tests use a neutral attribution callback by default so they can
          # test Gateway behavior independently, then explicitly defeat that boundary.
          p = Path('services/gateway/tests/test_gateway_slice.py')
          text = p.read_text(encoding='utf-8')
          text = text.replace(
              'CAPABILITY_MAP, MANIFESTS, CAPABILITY_TABLE = load_surface(ROOT)\n\n\nclass GatewayVerticalSlice',
              '''CAPABILITY_MAP, MANIFESTS, CAPABILITY_TABLE = load_surface(ROOT)


class AttributionDenied(RuntimeError):
    reason_code = "ACTOR_ATTRIBUTION_MISMATCH"


class GatewayVerticalSlice''', 1)
          text = text.replace(
              '''    def new_gateway(self, capability_map: dict | None = None,
                    manifests: dict | None = None,
                    authority: Callable[[str, str, str], str] | None = None,
                    routes: dict | None = None) -> Gateway:
''',
              '''    def new_gateway(self, capability_map: dict | None = None,
                    manifests: dict | None = None,
                    authority: Callable[[str, str, str], str] | None = None,
                    attribution: Callable[[str, str, str, str, str | None], None] | None = None,
                    routes: dict | None = None) -> Gateway:
''', 1)
          text = text.replace(
              '''        if routes is None:
            routes = {"asset:in-process": AssetRoutes(self.asset).call}
        return Gateway(
''',
              '''        if attribution is None:
            attribution = lambda *_: None
        if routes is None:
            routes = {"asset:in-process": AssetRoutes(self.asset).call}
        return Gateway(
''', 1)
          text = text.replace(
              '''            manifests or self.manifests, self.capability_table,
            authority, routes, authority_denials=(AuthorityRefused,),
''',
              '''            manifests or self.manifests, self.capability_table,
            authority, routes, attribution=attribution,
            authority_denials=(AuthorityRefused,),
            attribution_denials=(AttributionDenied,),
''', 1)
          text = text.replace(
              '''    def request(self, actor: str, actor_kind: str, path: Path, scope: str) -> dict:
        return {
            "actor": actor,
            "actor_kind": actor_kind,
''',
              '''    def attribution_fields(self, actor: str, actor_kind: str) -> dict:
        return {
            "session_id": f"session:{actor}",
            "session_binding_id": f"host:{actor_kind.lower()}",
            "principal_id": f"principal:{actor}",
            "interface_binding_id": f"interface:{actor_kind.lower()}",
            "interface_operation_digest": "a" * 64,
        }

    def request(self, actor: str, actor_kind: str, path: Path, scope: str) -> dict:
        return {
            "actor": actor,
            "actor_kind": actor_kind,
            **self.attribution_fields(actor, actor_kind),
''', 1)
          text = text.replace(
              '''        return {
            "actor": actor,
            "actor_kind": actor_kind,
            "logical_endpoint": "sov://asset/read-version",
''',
              '''        return {
            "actor": actor,
            "actor_kind": actor_kind,
            **self.attribution_fields(actor, actor_kind),
            "logical_endpoint": "sov://asset/read-version",
''', 1)
          text = text.replace(
              '''        request = {
            "actor": actor,
            "actor_kind": "HUMAN",
            "logical_endpoint": "sov://asset/request-derivative",
''',
              '''        request = {
            "actor": actor,
            "actor_kind": "HUMAN",
            **self.attribution_fields(actor, "HUMAN"),
            "logical_endpoint": "sov://asset/request-derivative",
''', 1)
          anchor = '''    def test_human_and_model_take_same_kernel_path_and_get_service_receipts_unchanged(self) -> None:
'''
          tests = '''    def test_session_attribution_refusal_is_durable_and_precedes_authority(self) -> None:
        authority_called = False

        def authority(*_: str) -> str:
            nonlocal authority_called
            authority_called = True
            return "grant_should_not_be_read"

        def deny(*_: object) -> None:
            raise AttributionDenied("session attribution mismatch")

        gateway = self.new_gateway(authority=authority, attribution=deny)
        refused = gateway.dispatch(
            self.request("operator", "HUMAN", self.source("attribution-denied.txt"),
                         "asset:new"))

        self.assertEqual(refused["payload"]["outcome"], "REFUSED")
        self.assertEqual(self.reason(refused), "ACTOR_ATTRIBUTION_MISMATCH")
        self.assertFalse(authority_called)
        self.assertEqual(self.asset.receipts(), [])
        self.assertEqual(self.gateway_events("gateway-authority-check"), [])
        check = self.gateway_events("gateway-session-attribution")[-1]["payload"]
        self.assertEqual(check["decision"], "REFUSED")
        self.assertEqual(check["diagnostic_code"], "ACTOR_ATTRIBUTION_MISMATCH")

    def test_session_attribution_reader_failure_is_operational_not_denial(self) -> None:
        def broken(*_: object) -> None:
            raise RuntimeError("session journal unavailable")

        failed = self.new_gateway(attribution=broken).dispatch(
            self.request("operator", "HUMAN", self.source("attribution-fault.txt"),
                         "asset:new"))

        self.assertEqual(failed["payload"]["outcome"], "FAILED")
        self.assertEqual(self.reason(failed), "ATTRIBUTION_CHECK_FAILED")
        self.assertEqual(self.detail(failed)["failure_class"], "OPERATIONAL_FAULT")
        self.assertEqual(self.gateway_events("gateway-authority-check"), [])
        check = self.gateway_events("gateway-session-attribution")[-1]["payload"]
        self.assertEqual(check["decision"], "FAILED")
        self.assertEqual(check["diagnostic_code"], "RuntimeError")

    def test_request_and_attribution_records_preserve_both_binding_layers(self) -> None:
        scope = self.grant_ingest()
        request = self.request("operator", "HUMAN", self.source("attributed.txt"), scope)
        returned = self.gateway.dispatch(request)
        self.assertEqual(returned["outcome"], "COMMITTED")

        claimed = self.gateway_events("gateway-request")[-1]["payload"]
        checked = self.gateway_events("gateway-session-attribution")[-1]["payload"]
        for field in ("session_id", "session_binding_id", "principal_id",
                      "interface_binding_id", "interface_operation_digest"):
            self.assertEqual(claimed[field], request[field])
            self.assertEqual(checked[field], request[field])
        self.assertNotEqual(request["session_binding_id"], request["interface_binding_id"])
        self.assertEqual(checked["decision"], "ALLOWED")

    def test_session_identity_is_required_before_attribution(self) -> None:
        request = self.request("operator", "HUMAN", self.source("sessionless.txt"), "asset:new")
        request.pop("session_id")
        refused = self.gateway.dispatch(request)
        self.assertEqual(self.reason(refused), "MALFORMED_REQUEST")
        self.assertEqual(self.gateway_events("gateway-session-attribution"), [])
        self.assertEqual(self.gateway_events("gateway-authority-check"), [])
        self.assertEqual(self.asset.receipts(), [])

'''
          if anchor not in text:
              raise SystemExit('gateway test anchor missing')
          text = text.replace(anchor, tests + anchor, 1)
          p.write_text(text, encoding='utf-8', newline='\n')

          # Node Interface proof opens a real Console session for every operation and
          # carries that session separately from the interface binding.
          replace(
              'scripts/sovnode/proof.py',
              'from sovnode.bindings import HUMAN, MODEL, invocation_request, render_human, render_model, resolve\n',
              'from sovnode.bindings import (\n    BINDING_IDS, HUMAN, MODEL, invocation_request, render_human, render_model, resolve,\n)\n')
          replace(
              'scripts/sovnode/proof.py',
              '''def _reason(receipt: dict[str, Any]) -> str | None:
    payload = receipt.get("payload")
    if isinstance(payload, dict):
        detail = payload.get("detail")
        if isinstance(detail, dict):
            return detail.get("reason_code")
    return None


def run() -> dict[str, Any]:
''',
              '''def _reason(receipt: dict[str, Any]) -> str | None:
    payload = receipt.get("payload")
    if isinstance(payload, dict):
        detail = payload.get("detail")
        if isinstance(detail, dict):
            return detail.get("reason_code")
    return None


def _open_session(node: LocalActionPath, actor: str, actor_kind: str) -> dict[str, Any]:
    issuer = "interface-proof-given"
    node.console.grant(actor, "open:session", actor, granted_by=issuer)
    host_binding = f"urn:soveraeign:binding:proof-host:{actor_kind.lower()}"
    return node.console.open_session(
        actor, actor_kind, host_binding, f"principal:{actor}")


def _request(node: LocalActionPath, document: dict[str, Any], operation_id: str,
             binding_kind: str, actor: str, scope: str, arguments: dict[str, Any],
             session: dict[str, Any]) -> dict[str, Any]:
    return invocation_request(
        document, operation_id, binding_kind, actor, scope, arguments,
        session_id=session["session_id"],
        session_binding_id=session["binding_id"],
        principal_id=session["principal_id"])


def _attribution_was_recorded(node: LocalActionPath, request: dict[str, Any]) -> bool:
    rows = [entry["payload"] for entry in node.record.entries()
            if entry["kind"] == "EVENT"
            and entry["payload"].get("record_kind") == "gateway-session-attribution"]
    if not rows:
        return False
    row = rows[-1]
    return (row.get("decision") == "ALLOWED"
            and all(row.get(field) == request.get(field) for field in (
                "session_id", "session_binding_id", "principal_id",
                "interface_binding_id", "interface_operation_digest")))


def run() -> dict[str, Any]:
''')
          replace(
              'scripts/sovnode/proof.py',
              '''            with LocalActionPath(root / binding_kind.lower()) as node:
                node.console.grant(actor, operation["required_authority"], scope,
                                   granted_by="interface-proof-given")
                request = invocation_request(
                    document, operation["operation_id"], binding_kind, actor, scope,
                    {"path": str(source), "label": "Interface proof"})
                returned = node.dispatch(request)
                durable = node.asset.receipts()[-1]
                detail = json.loads(returned["payload_json"])
                action_results[binding_kind] = {
                    "binding_id": request["binding_id"],
''',
              '''            with LocalActionPath(root / binding_kind.lower()) as node:
                session = _open_session(node, actor, binding_kind)
                node.console.grant(actor, operation["required_authority"], scope,
                                   granted_by="interface-proof-given")
                request = _request(
                    node, document, operation["operation_id"], binding_kind, actor, scope,
                    {"path": str(source), "label": "Interface proof"}, session)
                returned = node.dispatch(request)
                durable = node.asset.receipts()[-1]
                detail = json.loads(returned["payload_json"])
                action_results[binding_kind] = {
                    "interface_binding_id": request["interface_binding_id"],
                    "session_attribution_recorded": _attribution_was_recorded(node, request),
''')
          replace(
              'scripts/sovnode/proof.py',
              '''            with LocalActionPath(root / f"registry-{binding_kind.lower()}") as node:
                node.console.grant(actor, registry_operation["required_authority"],
                                   "registry:any", granted_by="interface-proof-given")
                request = invocation_request(
                    document, registry_operation["operation_id"], binding_kind,
                    actor, "registry:any", {"name": "sov://asset/ingest-asset"})
                returned = node.dispatch(request)
''',
              '''            with LocalActionPath(root / f"registry-{binding_kind.lower()}") as node:
                session = _open_session(node, actor, binding_kind)
                node.console.grant(actor, registry_operation["required_authority"],
                                   "registry:any", granted_by="interface-proof-given")
                request = _request(
                    node, document, registry_operation["operation_id"], binding_kind,
                    actor, "registry:any", {"name": "sov://asset/ingest-asset"}, session)
                returned = node.dispatch(request)
''')
          replace(
              'scripts/sovnode/proof.py',
              '''                registry_results[binding_kind] = {
                    "binding_id": request["binding_id"],
''',
              '''                registry_results[binding_kind] = {
                    "interface_binding_id": request["interface_binding_id"],
                    "session_attribution_recorded": _attribution_was_recorded(node, request),
''')
          replace(
              'scripts/sovnode/proof.py',
              '''            with LocalActionPath(
                    root / f"host-{binding_kind.lower()}",
                    host_adapter=_ProofHostAdapter()) as node:
                node.console.grant(actor, host_operation["required_authority"],
                                   "host:local", granted_by="interface-proof-given")
                request = invocation_request(
                    document, host_operation["operation_id"], binding_kind,
                    actor, "host:local", {})
                returned = node.dispatch(request)
''',
              '''            with LocalActionPath(
                    root / f"host-{binding_kind.lower()}",
                    host_adapter=_ProofHostAdapter()) as node:
                session = _open_session(node, actor, binding_kind)
                node.console.grant(actor, host_operation["required_authority"],
                                   "host:local", granted_by="interface-proof-given")
                request = _request(
                    node, document, host_operation["operation_id"], binding_kind,
                    actor, "host:local", {}, session)
                returned = node.dispatch(request)
''')
          replace(
              'scripts/sovnode/proof.py',
              '''                host_results[binding_kind] = {
                    "binding_id": request["binding_id"],
''',
              '''                host_results[binding_kind] = {
                    "interface_binding_id": request["interface_binding_id"],
                    "session_attribution_recorded": _attribution_was_recorded(node, request),
''')
          replace(
              'scripts/sovnode/proof.py',
              '''        with LocalActionPath(root / "refused") as node:
            request = invocation_request(
                document, operation["operation_id"], HUMAN, "interface-refused", "asset:new",
                {"path": str(source), "label": "Refused proof"})
            refusal = node.dispatch(request)

        with LocalActionPath(root / "inactive") as node:
            inactive = node.dispatch({
                "actor": "interface-human", "actor_kind": "HUMAN",
                "logical_endpoint": "sov://console/resolve-judgement",
                "transport": "IN_PROCESS", "scope": "judgement:any", "arguments": {},
            })
''',
              '''        with LocalActionPath(root / "refused") as node:
            session = _open_session(node, "interface-refused", HUMAN)
            request = _request(
                node, document, operation["operation_id"], HUMAN, "interface-refused",
                "asset:new", {"path": str(source), "label": "Refused proof"}, session)
            refusal = node.dispatch(request)
            refusal_attribution = _attribution_was_recorded(node, request)

        with LocalActionPath(root / "inactive") as node:
            actor = "interface-human"
            session = _open_session(node, actor, HUMAN)
            inactive_operation = resolve(document, "console.resolve-judgement")
            inactive_request = {
                "actor": actor, "actor_kind": HUMAN,
                "session_id": session["session_id"],
                "session_binding_id": session["binding_id"],
                "principal_id": session["principal_id"],
                "interface_binding_id": BINDING_IDS[HUMAN],
                "interface_operation_digest": inactive_operation["record_digest"],
                "logical_endpoint": "sov://console/resolve-judgement",
                "transport": "IN_PROCESS", "scope": "judgement:any", "arguments": {},
            }
            inactive = node.dispatch(inactive_request)
            inactive_attribution = _attribution_was_recorded(node, inactive_request)
''')
          replace(
              'scripts/sovnode/proof.py',
              '''        kind: {name: value for name, value in result.items()
               if name not in ("binding_id", "terminal_receipt_id")}
''',
              '''        kind: {name: value for name, value in result.items()
               if name not in ("interface_binding_id", "terminal_receipt_id")}
''')
          # There are three identical semantic-signature comprehensions.
          p = Path('scripts/sovnode/proof.py')
          text = p.read_text(encoding='utf-8')
          text = text.replace(
              'if name not in ("binding_id", "terminal_receipt_id")',
              'if name not in ("interface_binding_id", "terminal_receipt_id")')
          text = text.replace(
              '''        "refusal": {
            "outcome": refusal["payload"]["outcome"],
''',
              '''        "refusal": {
            "session_attribution_recorded": refusal_attribution,
            "outcome": refusal["payload"]["outcome"],
''', 1)
          text = text.replace(
              '''        "inactive_operation": {
            "operation_id": "console.resolve-judgement",
''',
              '''        "inactive_operation": {
            "session_attribution_recorded": inactive_attribution,
            "operation_id": "console.resolve-judgement",
''', 1)
          p.write_text(text, encoding='utf-8', newline='\n')

          # Pure Node Interface tests provide synthetic session facts because they do
          # not open runtime state. The live proof above covers the real join.
          p = Path('scripts/tests/test_node_interface.py')
          text = p.read_text(encoding='utf-8')
          marker = '            {"name": "sov://asset/ingest-asset"})'
          text = text.replace(marker, marker[:-1] + ', session_id="session:test",\n            session_binding_id="host:test", principal_id=None)', 1)
          text = text.replace(
              'invocation_request(edited, "asset.read-asset", HUMAN, "actor", "scope", {})',
              'invocation_request(edited, "asset.read-asset", HUMAN, "actor", "scope", {},\n                               session_id="session:test", session_binding_id="host:test",\n                               principal_id=None)', 1)
          text = text.replace(
              'synthetic, "asset.ingest-asset", MODEL, "model", "asset:new", {})',
              'synthetic, "asset.ingest-asset", MODEL, "model", "asset:new", {},\n                session_id="session:test", session_binding_id="host:test", principal_id=None)', 1)
          text = text.replace(
              'edited, "console.resolve-judgement", HUMAN, "actor", "scope", {})',
              'edited, "console.resolve-judgement", HUMAN, "actor", "scope", {},\n                session_id="session:test", session_binding_id="host:test", principal_id=None)', 1)
          text = text.replace(
              '''            self.assertTrue(result["service_receipt_unchanged"])
            self.assertEqual(result["operation_digest"], self.record["record_digest"])
''',
              '''            self.assertTrue(result["service_receipt_unchanged"])
            self.assertTrue(result["session_attribution_recorded"])
            self.assertEqual(result["operation_digest"], self.record["record_digest"])
''', 1)
          text = text.replace(
              '''            self.assertTrue(result["service_receipt_unchanged"])
            self.assertEqual(result["operation_digest"], record["record_digest"])
''',
              '''            self.assertTrue(result["service_receipt_unchanged"])
            self.assertTrue(result["session_attribution_recorded"])
            self.assertEqual(result["operation_digest"], record["record_digest"])
''', 2)
          text = text.replace(
              '''        self.assertEqual(self.proof["refusal"]["outcome"], "REFUSED")
''',
              '''        self.assertTrue(self.proof["refusal"]["session_attribution_recorded"])
        self.assertEqual(self.proof["refusal"]["outcome"], "REFUSED")
''', 1)
          text = text.replace(
              '''        self.assertFalse(inactive["interface_reachable"])
''',
              '''        self.assertTrue(inactive["session_attribution_recorded"])
        self.assertFalse(inactive["interface_reachable"])
''', 1)
          p.write_text(text, encoding='utf-8', newline='\n')
          PY
      - name: Rebuild projections touched by the manifest and route source
        run: |
          python scripts/sov_capability.py build
          python scripts/sov_interface.py build
          python scripts/sov_surface.py render
          python scripts/sov_docs.py build
      - name: Run focused attribution and interface tests
        run: |
          python -m unittest services.gateway.tests.test_gateway_slice
          python -m unittest scripts.tests.test_node_interface
      - name: Verify repository
        run: |
          python scripts/sov_capability.py check
          python scripts/sov_interface.py check
          python scripts/sov_surface.py check
          python scripts/sov_docs.py check
          python scripts/verify.py
      - name: Commit the gap slice
        run: |
          if git diff --quiet; then
            echo 'No changes.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            services/gateway/src/soveraeign_gateway_service/contract.py \
            services/gateway/src/soveraeign_gateway_service/evidence.py \
            services/gateway/src/soveraeign_gateway_service/core.py \
            services/gateway/contracts/service.json \
            services/gateway/tests/test_gateway_slice.py \
            scripts/sovnode/composition.py scripts/sovnode/bindings.py scripts/sovnode/proof.py \
            scripts/tests/test_node_interface.py \
            contracts/fixtures/capability-map.reference.json \
            contracts/fixtures/node-interface.reference.json \
            docs/surface.html docs/documentation.html
          git commit -m 'feat(gap): bind operation crossings to isolated sessions'
          git push
