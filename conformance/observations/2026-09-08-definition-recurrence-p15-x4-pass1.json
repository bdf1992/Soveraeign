{
  "case_id": "WITNESS-2026-09-08-DEFINITION-RECURRENCE-P15-X4",
  "record_kind": "OBSERVATION",
  "record_note": "An observation, not a settlement and not a acceptance. Written under .local/ at the launching agent's direction because witness/ and reports/ sit outside grant:standing-landing-loop.",
  "participant_id": "claude-opus-5/sov-witness@2026-09-08",
  "independence": {
    "contributed_to_build": false,
    "edited_subject": false,
    "helpers_used": "none; second reading done in-session",
    "read_builder_conclusions_before_freeze": "the commit message was read to locate the subject; no Orchestrator or Controller evaluative conclusion was read before this Finding froze"
  },
  "subject": {
    "work": "scripts/sov_recurrence.py + scripts/sovrecurrence/, admitted to custody:phase-1-5/definition-recurrence (P15-X4) at standing BUILT",
    "artifact_revision": "fe91c655ec4897bc04423d59fcbeae549cfb8d2b",
    "baseline_revision": "d00f46ad38448856fa692399d12cf0a324266004",
    "branch": "claude/sovereign-phase-1-5-exits-fb7ysc",
    "working_tree_state": "clean; git status --porcelain returned 0 lines before and after every measurement; all measurement taken in an isolated detached worktree at fe91c65 to satisfy trap T6"
  },
  "record_projection": {
    "supplied": null,
    "reconstructed_as": "the committed tree at fe91c65 and its first parent d00f46a",
    "cutoff": "fe91c655ec4897bc04423d59fcbeae549cfb8d2b",
    "note": "No RecordProjection id was supplied and none is invented. Every address cited below was resolved from the commit tree at the declared cutoff."
  },
  "content_digests_sha256_16": {
    "scripts/sov_recurrence.py": "7fb026cd97a3ecfd",
    "scripts/sovrecurrence/experience.py": "4538b54887bed9e3",
    "scripts/sovrecurrence/candidate.py": "0974774ff6701029",
    "scripts/sovrecurrence/neutrality.py": "9a7c8416ecd89307",
    "scripts/sovrecurrence/fixture.py": "5d1ce51416d5aa7f",
    "scripts/sovrecurrence/recurrence.py": "7416cdeac1cddfc7",
    "scripts/sovverify/commissioning.py": "4fbc294898400254",
    "contracts/custodies/phase-1-5.json": "3b0d90e6d6c4434c",
    "scripts/README.md": "176cc8940aa52a4f"
  },
  "commands": [
    {"cmd": "python3 scripts/sov_recurrence.py selfcheck", "cwd": "fe91c65 worktree", "exit": 0},
    {"cmd": "python3 scripts/sov_recurrence.py run", "cwd": "fe91c65 worktree", "exit": 0},
    {"cmd": "python3 -m unittest -q scripts.tests.test_sov_recurrence", "cwd": "fe91c65 worktree", "exit": 0},
    {"cmd": "python3 scripts/verify.py", "cwd": "fe91c65 worktree", "exit": 1, "runs": 2, "note": "deterministic across both runs; 49 FAIL lines"},
    {"cmd": "python3 scripts/verify.py", "cwd": "d00f46a worktree", "exit": 0, "note": "47 FAIL lines"},
    {"cmd": "python3 scripts/lint.py", "cwd": "fe91c65 worktree", "exit": 0},
    {"cmd": "python3 scripts/sov_active_phase_progress.py", "cwd": "fe91c65 worktree", "exit": 0}
  ],
  "verdicts": {
    "claim_1_clause_was_never_an_owner_question": "REPRODUCED",
    "claim_2_basis_is_settled_experience": "REPRODUCED (admission logic); PARTIAL (untested paths, see F3/F5)",
    "claim_3_candidate_takes_no_standing": "REPRODUCED (behaviour); DISSENTED (the guard has no defeating fixture, see F2)",
    "claim_4_neutrality_is_measured": "PARTIALLY REPRODUCED (see F6, F7, F8)",
    "claim_5_the_instrument_can_fail": "DISSENTED (four planted defects survive the whole delivered surface, see F2/F3/F4/F5)",
    "claim_6_nothing_was_broken": "DISSENTED (verify.py exits 1 at fe91c65 and 0 at its parent, see F1)",
    "claim_7_p15_x3_registration": "PARTIALLY REPRODUCED (see F9)"
  },
  "findings": [
    {"id": "F1", "severity": "BLOCKING", "address": "scripts/README.md:3", "claim_defeated": "Verify PASS (57 checks)", "observed": "verify.py exits 1 at fe91c65 and 0 at d00f46a; the delta is exactly `FAIL scripts/README.md:3: states 60 sov_*.py entrypoints, the record holds 61 (command-entrypoints)` and its `FAIL: counted populations` summary. This change added scripts/sov_recurrence.py and did not update the count."},
    {"id": "F2", "severity": "MATERIAL", "address": "scripts/sovrecurrence/fixture.py:run_variant policy-moved; scripts/sovrecurrence/recurrence.py:observe", "observed": "The before/after governing-record digest guard has no defeating fixture. mutate() is applied after `after` is computed and the policy-moved variant sets automatic_policy_change/automatic_phase_transition directly. Setting candidate.GOVERNING = () leaves selfcheck, run and the unit tests all green."},
    {"id": "F3", "severity": "MATERIAL", "address": "scripts/sovrecurrence/experience.py:digest directory branch", "observed": "Deleting `rolling.update(entry.read_bytes())` makes the tree digest byte-blind for services/observation; selfcheck, run and unit tests stay green. The shipped digest is real (control confirmed) but untested."},
    {"id": "F4", "severity": "MATERIAL", "address": "scripts/sovrecurrence/experience.py:gather", "observed": "Changing `member.get(\"work_state\")` to `member.get(\"work_state\", LANDED)` admits any member that omits work_state as settled experience; selfcheck, run and unit tests stay green. The unit test sets work_state explicitly, so a defaulted-on LANDED half is invisible."},
    {"id": "F5", "severity": "MATERIAL", "address": "conformance/commissioning.py:check_q41; scripts/sovrecurrence/recurrence.py:observe", "observed": "In the live path settled_evidence_addresses and candidate.source_addresses are the same list, so basis.issubset(source) is X.issubset(X) and cannot fail. Killing EVIDENCE_PATH shrinks the live basis 8 -> 3 sources and the reading still reports PASS. This is the exact silent-shrink the experience.py docstring says P15-Q4.1 exists to catch."},
    {"id": "F6", "severity": "MATERIAL", "address": "scripts/sovrecurrence/neutrality.py:read", "observed": "The closed-vocabulary scan covers only the ten bound files. A closure in any other contract is invisible. Zero closed enums exist anywhere in contracts/, so P15-Q4.3 is vacuously satisfied against the real repository."},
    {"id": "F7", "severity": "MINOR", "address": "scripts/sovrecurrence/neutrality.py:_closed_enums", "observed": "The detector misses: enum of the four plus one extra name; JSON Schema `const`; a regex `pattern` closing to the four; required-property closure. Live counterexample: contracts/issue-metadata.schema.json /properties/requested_by = [worker, orchestrator, controller, owner] is graded neutral."},
    {"id": "F8", "severity": "MINOR", "address": "scripts/sovrecurrence/neutrality.py:PRIMITIVES['session']", "observed": "`session` is bound to contracts/source.schema.json, which declares the SPEC Source object, an immutable captured input. No session contract exists. The reader reports 10 of 10 primitives resolved while one has no governing declaration."},
    {"id": "F9", "severity": "MINOR", "address": "scripts/sovverify/commissioning.py:25-32", "observed": "True that nothing ran the CLI subcommand `sov_reuse.py selfcheck`. Overstated as the Phase-I lesson: scripts/tests/test_sov_reuse.py::DefeatingVariants exercised the same fixture variants in-process on main and is in the tooling suite. What was missing was the entry point, not the grading."},
    {"id": "F10", "severity": "MINOR", "address": "scripts/sovverify/commissioning.py `observes` tuples", "observed": "sov_recurrence.py selfcheck reads zero bytes under the repository root while declaring four repository addresses as observed, including contracts/custodies/phase-1-5.json. Same for the sov_reuse and pre-existing sov_fresh checks. scripts/sovland/isolation.py:43 already records that nothing grades the tuple. Pattern inherited, extended twice without flagging."},
    {"id": "F11", "severity": "MINOR", "address": "contracts/verification-budget.json 'discovery and reuse slice': 3.0", "observed": "Measured 4.750s wall on this host, entering the suite already in budget debt. Host-dependent; recorded as evidence, not a refusal."},
    {"id": "F12", "severity": "LATENT", "address": "scripts/sovrecurrence/experience.py:WITNESSED", "observed": "Admission is equality on WITNESSED, not `at least witnessed`. A member at ACCEPTED would be silently dropped from settled experience. No ACCEPTED member exists today."}
  ],
  "reproduced": [
    "decisions/0102-open-phase-1-5.md Decision item 3 gives every clause a live EXIT custody from the first day; its Judgement queue for Bdo reads None",
    "contracts/acceptance-policy.json hold_reasons is exhaustive and admits no reason to escalate a RECORD_LOCAL revertible member admission; escalation would be refused by PREAPPROVAL_REQUESTED",
    "experience.gather refuses BUILT, PRESENTED, ACCEPTED, lowercase, whitespace-suffixed, NOT_WITNESSED, PRE_WITNESSED and WITNESSED_PARTIAL; trap T3 avoided by whole-token equality",
    "the live reading cites 8 sources across P15-X1/X2/X3 and does not cite scripts/sov_recurrence.py itself",
    "the directory tree-digest detects a byte change inside services/observation",
    "run and selfcheck perform zero writes; the whole tree is byte-identical across both; git status stays clean",
    "conformance/commissioning.py imports only __future__ and typing; it predates this change at d06243c, an ancestor of origin/main",
    "the prose-versus-enum distinction is real: a prose mention of all four roles does not defeat; an enum of the four does, case-insensitively",
    "lint PASS; no unrelated files; no secrets; every new module under 300 lines",
    "the two new commissioning checks are actually invoked by verify.py",
    "scripts/tests/test_sov_recurrence.py is in the 114-module tooling suite and its AgainstThisRepository cases do read the real repository"
  ],
  "unattestable": [],
  "standing_supported": "NONE. BUILT -> WITNESSED is not supported at fe91c65."
}
