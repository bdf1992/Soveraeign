"""Grading the closure check a custody declares: can somebody actually run it.

Distinct from `sovclosure`, which reads the closure-ownership table and judges a
handoff against it. That package answers "who carries this concern"; this one
answers "does the command this custody names dispatch when a person types it".
"""
