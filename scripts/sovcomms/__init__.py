"""Detectors for the Communications claim surface.

`scripts/sov_comms.py` is the entry point; the kinds live here so the grader
stays inside the module budget as kinds are added.
"""

from __future__ import annotations
