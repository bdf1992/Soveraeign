"""Fresh participation probe: the P15-X1 vertical slice, run against the real layers.

A participant with no private history enters from the artifact, resolves who it is,
what campaign is open, what bounded work it may carry, what it can reach and under
what authority, and receives a Record projection of its own entry. The probe drives
the same modules the session, lease, node-interface, authority and Record surfaces
use, and grades the result through `conformance/commissioning.py`, which imports no
participant code. The probe proves the slice is closed; it does not witness it.
"""
