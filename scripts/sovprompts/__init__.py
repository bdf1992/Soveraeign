"""Reading what a workflow hands an agent, and whether the file it lives in holds.

Named for what it reads rather than for where the files sit: `scripts/sovharness/` is
taken by the harness-claims grader on another branch, which answers a different question
about the same directory.
"""

from __future__ import annotations
