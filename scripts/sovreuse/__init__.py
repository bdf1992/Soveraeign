"""Discovery and reuse of a result that stands: the P15-X3 vertical slice.

`discover` reads the artifact, `settle` grades how the result settled (P15-Q3.1), and
`reuse` reaches and uses what it produced (P15-Q3.2). None of them witnesses anything.
"""
