[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "soveraeign-gateway-service"
version = "0.1.0"
description = "Transport-neutral governed routing for a Soveraeign node"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
